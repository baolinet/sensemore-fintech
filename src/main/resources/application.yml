server:
    port: 8080

ai:
  api:
    url: ${PROD_API_URL:https://api-77aaidn1l8c5b7xa.aistudio-app.com/v1/chat/completions}
    authToken: ${PROD_API_TOKEN:115925abb19ec543cdcbe8af4506ff463ea2b5e8}

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sensemore?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: Qa123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  ai:
    mcp:
      client:
        streamable-http:
          connections:
            ditu-server:
              url: https://mcp.amap.com
              endpoint: /mcp?key=da1014004203e42146f4458c78df87ae
    chat:
      observations:
        log-prompt: true # Include prompt content in tracing (disabled by default for privacy)
        log-completion: true # Include completion content in tracing (disabled by default)
    openai:
      api-key: "115925abb19ec543cdcbe8af4506ff463ea2b5e8"
      base-url: https://api-77aaidn1l8c5b7xa.aistudio-app.com
      #  兼容其他OpenAI格式的大模型配置示例
      chat:
        options:
          # 模型ID,需要替换为实际的接入点ID
          model: gemma3:27b

management:
  tracing:
    sampling:
      probability: 1.0 # Sample 100% of requests for full tracing (adjust in production as needed)
  observations:
    annotations:
      enabled: true       # Enable @Observed (if you use observation annotations in code)

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.sensemore.loan.infra.mysql.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl